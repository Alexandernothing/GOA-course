// 6. შექმენით სია სადაც იქნება მინიმუმ 5 განსხვავებული ელემენტი. ზოგიერთი ელემენტი უნდა მეორდებოდეს. შემდეგ შექმენით ობიექტი სადაც ჩაამატებთ ამ ელემენტს როგორც key და მისი მნიშვნელობა იყოს რამდენჯერაც გვხვდება სიაში.

let list = [ 1, 1, 2, 3, 3, 4, 3, 5]

let count = {

}

for (let i of list) {
    if (i in count) {
        count[i] += 1
    } 
    else {
        count[i] = 1
    }
}

console.log(count)